# Portfolio v1.2.7

Privacy-friendly A/B testing for the NecrotixLab homepage.

## Experiments

Three independent 50/50 experiments now run on the homepage:

- **Niko intro duration**
  - A: current 2.5 second first-visit intro
  - B: faster 1.8 second first-visit intro
  - Primary metric: engaged session
- **Homepage section order**
  - A: Journal before Projects
  - B: Projects before Journal
  - Primary metric: project open
- **Hero micro navigation**
  - A: current hero without text CTA
  - B: discreet `View projects` and `Explore gallery` links
  - Primary metric: project open

The hero component itself is not redesigned. Variant B adds a lightweight overlay from `HomeClient`, so the protected HeroVisual layout remains untouched.

## Privacy model

- Variant assignment is stored only in `sessionStorage`.
- A visitor remains in the same variant for the current browser session.
- Exposure and conversion events are de-duplicated per session.
- The server stores aggregate counters only.
- No experiment visitor ID, name, email address or IP address is stored in the experiment table.
- Same-origin validation is applied to the public event endpoint.
- Privacy and Cookie policies document the experiment storage and aggregate measurement.

## Admin

Adds **Admin > Tools > Experiments** for OWNER and ADMIN roles.

The dashboard:

- refreshes automatically every 5 seconds;
- shows A/B exposures and primary conversion rates;
- calculates B vs A lift;
- shows supporting engagement events;
- avoids declaring a winner when the sample is too small.

## Database

Adds `ExperimentMetric` with aggregate counters keyed by:

- experiment ID
- variant
- event

Migration: `20260904065000_experiment_metrics`.

## Release

- Version `1.2.7`.
- Admin updater-ready after merge through `package.json`.
- Prisma migration required and compatible with the normal production migration flow.
- No new dependencies.
