# v1.1.61 - Social Preview & LinkedIn Fix

- Normalizes Media Library social images to absolute public HTTP(S) URLs before emitting metadata.
- Uses `https://necrotixlab.com` as the safe production metadata base when `NEXT_PUBLIC_SITE_URL` is unavailable.
- Adds 1200x630 Open Graph/Twitter image descriptors with alt text for more reliable social crawlers.
- Improves homepage fallback order: Open Graph image -> Default social image.
- Improves X/Twitter fallback order: Twitter image -> Open Graph image -> Default social image.
- Applies the same social image handling to Blog posts, Projects and CMS Pages.
- CMS Pages now inherit the global social image when no page featured image is available.
- Keeps social metadata generated by the existing Admin > SEO and Media Library system; no manual meta tags are required.
- No Prisma migration is required.
