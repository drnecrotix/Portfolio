# Portfolio v1.2.49

## Digital Footprint results and account discovery

- Uses one field with automatic email, phone or username detection.
- Replaces large result cards with compact rows and a detailed click-through panel.
- Adds result filtering, timeline ordering and provider coverage statuses.
- Deduplicates matching breach records while preserving provider attribution and exposed-field categories.
- Expands verified public username account links across GitHub, GitLab, Codeberg, Reddit, DEV Community, Keybase, Hacker News and npm.
- Adds public GitLab account matching for explicitly exposed email addresses.
- Removes the password-entry checker and its API route.
- Shows whether each provider is available, unsupported for the detected identifier or not configured.
