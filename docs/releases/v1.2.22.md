# Portfolio v1.2.22

## Live traffic analytics

- Adds a lightweight 60-second visitor heartbeat so the live visitor count stays accurate while someone remains on the same page.
- Tracks only the current public pathname inside the short-lived traffic session record.
- Shows which public pages are being viewed right now and how many active visitors are on each page.
- Shows live visitors by country without collecting city or precise-location data.
- Heartbeats refresh live activity without incrementing page-view totals every minute.

## Clearer traffic overview

- Replaces ambiguous `Page opens`, `Session depth`, `Share of traffic` and `Country coverage` summary cards with visitor-focused information.
- Shows visitors online now, active pages, total visits for the selected period and countries that generated visits.
- Replaces the weekday-average chart with a direct daily visits chart for the selected 7-day or 30-day period.
- Adds an exact `Visits by country` table with the number of visits and currently online visitors per country.
- Keeps the world map as a secondary visualization while putting exact country numbers beside it.
- Simplifies device analytics to visit counts instead of abstract page-share percentages.

## Database

- Adds nullable `TrafficSession.currentPath` for short-lived live-page reporting.
- Adds an index on `currentPath` and `lastSeenAt` for efficient live-page queries.
- Existing traffic metrics remain compatible and no historical traffic data needs to be rewritten.
