# Portfolio v1.2.47

## Admin-managed SMTP

- Adds encrypted SMTP configuration to Admin > API Integrations.
- Adds a built-in SMTP authentication test.
- Uses CMS-managed SMTP for Digital Footprint email verification and the contact form.
- Keeps environment variables as a deployment fallback.
